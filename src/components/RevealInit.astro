---
---

<noscript><style>.reveal-ov{opacity:1!important;transform:none!important}</style></noscript>

<script is:inline>
	(function () {
		function reducedMotion() {
			return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
		}
		function bind(el) {
			if (el.dataset.revealBound) return;
			el.dataset.revealBound = '1';
			if (reducedMotion()) {
				el.classList.add('reveal-ov--in');
				return;
			}
			// Reveal synchronously if already above the fold
			var rect = el.getBoundingClientRect();
			if (rect.top < window.innerHeight * 0.92) {
				el.classList.add('reveal-ov--in');
				return;
			}
			var io = new IntersectionObserver(
				function (entries) {
					entries.forEach(function (e) {
						if (e.isIntersecting) {
							e.target.classList.add('reveal-ov--in');
							io.unobserve(e.target);
						}
					});
				},
				{ threshold: 0.08, rootMargin: '0px 0px -8% 0px' },
			);
			io.observe(el);
		}
		function setup() {
			document.querySelectorAll('.reveal-ov').forEach(bind);
		}
		setup();
		document.addEventListener('astro:page-load', setup);
	})();
</script>
